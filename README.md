# Purpose:

This is a basic store shopping-cart example using indexedDB(localbase) to store cart state for the user. The app steps through the shopping cart process.
(This app has no stylling except for a few flexing and was just for the purpose of going through the logical steps.)
